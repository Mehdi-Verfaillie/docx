// dataNormalizer.ts

export function normalizeData(data: string): string {
  // Vous pouvez mettre en forme ou transformer les données ici
  // Par exemple, convertir en minuscules
  const normalizedData = data.toLowerCase();
  return normalizedData;
}
